---
import MainLayout from '@layouts/MainLayout.astro';
import { getLangStaticPaths, getUrlFromDestinationAndLang, type LangParams, useTranslations } from '@utils/i18n/utils';

export { getLangStaticPaths as getStaticPaths };

const { lang } = Astro.params as LangParams;
const t = useTranslations(lang);
---

<MainLayout title="title" description="description">
  <h1>Formation {lang}</h1>
  <a href={getUrlFromDestinationAndLang('', lang)}>Home</a>
</MainLayout>
