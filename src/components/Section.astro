---
import Wrapper from '@components/Wrapper.astro';
import { type TranslatedMessage } from '@utils/i18n/utils';

interface Props {
  id: string;
  isMainSection: boolean;
  title: TranslatedMessage;
}

const { isMainSection, id, title } = Astro.props;
---

<section id={id} class:list={[{ main: isMainSection }]}>
  <Wrapper>
    {!isMainSection && <h2>{title}</h2>}
    <slot />
  </Wrapper>
</section>

<style>
  section {
    margin-bottom: 6rem;
  }

  @media (width > 54em) {
    section {
      margin-bottom: 8rem;
    }
  }

  @media (width > 64em) {
    section {
      margin-bottom: 10rem;
    }
  }

  section.main {
    --header-padding-block: 1rem;
    --header-max-content-height: 2.5rem;
    margin-top: calc(2 * var(--header-padding-block) + var(--header-max-content-height));
  }

  @media (width > 54em) {
    section.main {
      min-height: 100svh;
      display: flex;
      align-items: center;
      margin: 0;
    }
  }

  h2 {
    text-align: center;
    font-size: 2rem;
    margin-bottom: 2rem;
  }

  @media (width > 54em) {
    h2 {
      font-size: 2.5rem;
      margin-bottom: 2.5rem;
    }
  }
</style>
